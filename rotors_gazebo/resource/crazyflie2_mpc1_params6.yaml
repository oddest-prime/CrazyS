# Swarm controller parameters
swarm: {
  neighbourhood_distance: 0.8, # approx 1m
  }
mpc1: {
  eps_move: 0.05, # 0.05,
  n_move_max: 6, # max 10 looks also good,
  mpc_cohesion_weight: 20.0,
  mpc_separation_weight: 6.0,
  mpc_target_weight: 150.0, # 50.0
  mpc_obstacle_weight: 2.5
  }
gradient: {
  scale_factor: 0.007 # 0.03
  }
inner: {
  controller: 2 # 1 -> PID, 2 -> explicit
  }
